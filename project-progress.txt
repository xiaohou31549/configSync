[2026-03-31 10:00 CST] Harness 基线建立
- 建立 AGENTS.md 目录索引与 docs/ 一级事实来源结构。
- 计划新增 init.sh、feature_list.json、文档园丁、架构边界检查、CI 和 macOS UI 自动化。
- 当前尚未完成验证，后续班次应先运行 ./init.sh 与全部校验脚本。

[2026-03-31 16:20 CST] Harness 第一轮落地完成
- 已新增 `init.sh`、`feature_list.json`、`scripts/doc_gardening.py`、`scripts/check_architecture.sh`、`scripts/run_ui_tests.sh`、`scripts/housekeeping_scan.sh` 和 GitHub Actions 工作流。
- 已把 Harness 环境注入收敛到 `HarnessRuntime`，并为关键 SwiftUI 控件补充可访问性标识。
- 已新增 macOS `XCUITest` 冒烟用例骨架，但默认不进入阻塞 CI；运行它需要提供 `TEAM_ID` 解决本机签名与 Runner 装载问题。
- 已验证通过：`python3 scripts/validate_feature_list.py feature_list.json`、`python3 scripts/doc_gardening.py --check`、`./scripts/check_architecture.sh`、`xcodebuild ... test`、`NO_LAUNCH=1 ./init.sh`。

[2026-03-31 22:02 CST] Harness 基线复核通过
- 已将占位命名的 Secret 加密实现重命名为正式服务，消除后台清理扫描的 `Placeholder` 命中。
- 已重新验证通过：`python3 scripts/validate_feature_list.py feature_list.json`、`python3 scripts/doc_gardening.py --check`、`./scripts/check_architecture.sh`、`./scripts/housekeeping_scan.sh --check`、`NO_LAUNCH=1 ./init.sh`。
- 已将 `feature_list.json` 中 `harness-engineering-baseline` 的 `passes` 更新为 `true`。

[2026-03-31 22:12 CST] App icon 已创建并接入
- 已新增 `Assets.xcassets/AppIcon.appiconset` 与图标生成脚本 `scripts/generate_app_icon.swift`，图标主题为“仓库堆叠 + 盾牌锁”。
- 已在 `project.yml` 中声明 `ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon`，重新生成工程后资源会自动进入应用目标。
- 已验证通过：`swift scripts/generate_app_icon.swift`、`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -configuration Debug -destination 'platform=macOS' -derivedDataPath build/DerivedData -clonedSourcePackagesDirPath build/SourcePackages build`，并确认产物包含 `Contents/Resources/AppIcon.icns`、`Contents/Resources/Assets.car`，且 `Info.plist` 含 `CFBundleIconName=AppIcon`。

[2026-04-01 14:42 CST] GitHub OAuth 与仓库选择链路补齐验证
- 已补充 GitHub OAuth 配置加载、loopback 回调、授权码换 token、会话恢复的单元测试，并加固回调监听就绪前的测试同步点，覆盖 `github-oauth-login-session`。
- 已补充 GitHub 仓库列表 API 解析、仓库排序，以及 `AppViewModel` 的搜索/筛选/多选行为测试，覆盖 `github-repo-fetch-search-select`。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test -only-testing:SecretSyncKitTests`。
- 已将 `feature_list.json` 中 `github-oauth-login-session` 与 `github-repo-fetch-search-select` 的 `passes` 更新为 `true`。

[2026-04-01 15:00 CST] 认证路线切换为 GitHub App
- 已根据产品决策，将 GitHub 仓库联动方案从 OAuth App 调整为 GitHub App 安装 + 用户授权 + 安装访问令牌。
- 已同步更新 `feature_list.json`、`docs/PRODUCT_SPEC.md`、`docs/ARCHITECTURE.md`、`docs/EXECUTION_PLAN.md`、`docs/HARNESS_ENGINEERING.md`、`docs/TESTING_STRATEGY.md`、`docs/DATABASE_SCHEMA.md` 的事实描述。
- 由于当前实现仍基于 OAuth，`github-app-install-login-session`、`github-repo-fetch-search-select` 与 `github-app-settings-persistence` 已按新范围回退为 `passes=false`，等待按 GitHub App 方案重做实现与验证。
- 本班次尚未开始 GitHub App 代码迁移，下一步应先完成 GitHub App 后台配置，再改本地配置模型、授权流程与仓库访问链路。

[2026-04-01 16:48 CST] GitHub App 认证与仓库链路完成首轮迁移
- 已将本地 GitHub 配置模型切换为 GitHub App 字段，并支持保存完整回调 URL 或仅保存回调 path。
- 已实现 GitHub App 用户令牌恢复、安装列表解析、安装访问令牌换取与缓存，仓库列表改为基于用户可访问安装聚合拉取。
- 已将仓库同步客户端改为按仓库 `installationID` 使用 installation token 发起请求，并补充对应单元测试。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test -only-testing:SecretSyncKitTests`。
- 已将 `feature_list.json` 中 `github-app-install-login-session`、`github-repo-fetch-search-select` 与 `github-app-settings-persistence` 更新为 `passes=true`。

[2026-04-01 16:53 CST] GitHub 同步 API 封装验证补齐
- 已为 GitHub Actions 同步客户端补充成功与失败分支测试，覆盖 Secret/Variable 上传成功、安装权限不足、仓库公钥异常与变量上传失败。
- 已通过测试固定 GitHub 仓库排序、搜索筛选和 GitHub App 安装令牌链路的行为，避免回归时遗漏安装令牌切换。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test -only-testing:SecretSyncKitTests`。
- 已将 `feature_list.json` 中 `github-sync-api-client` 更新为 `passes=true`。

[2026-04-01 16:59 CST] 同步前选择态与就绪校验完成
- 已在 `AppViewModel` 增加同步就绪状态计算，统一阻断未登录、未选仓库、未选配置项三类场景，并让同步按钮仅在真正可执行时可用。
- 已更新 `ConfigEditorView` 的同步提示与按钮禁用逻辑，避免未满足前置条件时误触发同步。
- 已补充单元测试覆盖阻断分支与放行分支，确认只有在登录、仓库和配置项都满足时才会发起同步执行。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test -only-testing:SecretSyncKitTests`。
- 已将 `feature_list.json` 中 `sync-selection-readiness` 更新为 `passes=true`。

[2026-04-01 17:06 CST] GitHub App 本地密钥存储改为 Keychain
- 已将 GitHub App 本地配置改为“文件存元数据、Keychain 存 `Client Secret`”，避免把敏感字段写入 `auth.json`。
- 已为配置加载增加旧版 `auth.json` 兼容逻辑；若历史文件仍包含 `Client Secret`，当前版本仍可读取。
- 已更新登录设置页文案，明确 `Client Secret` 进入 macOS Keychain，私钥文件继续使用用户提供的本地路径。
- 已同步更新 `docs/PRODUCT_SPEC.md` 与 `docs/DATABASE_SCHEMA.md` 的事实描述。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test -only-testing:SecretSyncKitTests`、`python3 scripts/validate_feature_list.py feature_list.json`。
[2026-04-01 09:30 CST] 本地 Secret CRUD MVP 已落地
- 已将第一版范围收敛为本地 Secret 增删改查，移除“导入示例配置”和 Variable 导向文案，明确新建仅支持在应用内本地创建。
- 已新增启动即加载本地 Secret 的流程，未登录 GitHub 时也能直接进入本地 Secret 编辑。
- 已补充保存重复名校验、编辑回写、删除幂等的单元测试，并将 UI 冒烟改为覆盖本地 Secret 的新增、编辑、删除路径。
- 已验证通过：`xcodebuild -project SecretSync.xcodeproj -scheme SecretSync -destination 'platform=macOS' test`、`python3 scripts/validate_feature_list.py feature_list.json`、`./scripts/check_architecture.sh`。
- 尚未执行：`TEAM_ID=你的开发团队ID ./scripts/run_ui_tests.sh`，当前环境缺少 macOS XCUITest 所需签名参数。

[2026-04-01 15:00 CST] UI 测试输入法切换优化已接入
- 已安装 `im-select`，并将 `scripts/run_ui_tests.sh` 更新为在执行前自动切换到系统输入法 `com.apple.keylayout.ABC`，退出后恢复原输入法。
- 此修改用于规避 macOS `XCUITest` 通过 `testmanagerd` 模拟输入时，第三方输入法触发的隐私授权弹窗；在当前环境中，原始输入法为 `com.tencent.inputmethod.wetype.pinyin`，脚本可正确切换并恢复。
- 已验证通过：`TEAM_ID=4374YTC7UU ./scripts/run_ui_tests.sh`，本地 Secret CRUD 的 UI 用例通过，且本轮日志中未再出现 `com.apple.systempreferences` 中断窗口。

[2026-04-01 16:10 CST] 修复 Code Review 的同步范围与 PID 工件问题
- 已修复 `AppViewModel`：仅在编辑器打开时才按单个选中 Secret 同步；初始化与刷新不再默认选中首项，避免“同步全部”被意外降级为首条。
- 已补充单元测试，覆盖“编辑器关闭时同步范围应为过滤列表”。
- 已删除误提交的 `.harness/app.pid`，并将 `.harness/*.pid`、`.harness/*.log` 加入 `.gitignore`，避免运行时工件再次入库。
- 验证结果：`python3 scripts/validate_feature_list.py feature_list.json`、`bash scripts/check_architecture.sh` 通过；`xcodebuild ... test` 因环境缺少 `xcodebuild` 未执行。
